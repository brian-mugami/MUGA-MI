def square(number):
  return number**number

def divide(number,dividend):
  return number / dividend


def add(number, number2):
  return number + number2

def minus(num1,num2):
  return num1-num2

done

